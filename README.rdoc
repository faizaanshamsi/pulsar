== README

Pulsar was created in partnership with Launch Academy, and was deployed at http://la-pulsar.herokuapp.com

It requires:

* Ruby >=2.0.0

* A valid application ID and secret from Github set as environment variables (see '.env.example') for use with OmniAuth.

In future versions we would like to implement:

* Pagination of recent learnings.
